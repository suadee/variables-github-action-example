name: Figma Design System Sync

on:
  repository_dispatch:
    types: [figma-library-publish] # Figma에서 보낼 이벤트 이름

jobs:
  update-variables:
    runs-on: ubuntu-latest
    steps:
      - name: Check Out Code
        uses: actions/checkout@v4

      - name: Process Figma Data
        run: |
          echo "Figma에서 날아온 데이터: ${{ github.event.client_payload.file_name }}"
          # 여기서 GitHub Variables API를 호출하거나 파일을 수정하는 로직을 수행합니다.
          # 예: 수신된 데이터를 바탕으로 theme.json 업데이트 등
